coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 4%
    patch:
      default:
        target: auto
        threshold: 4%

comment:
  layout: "reach, diff, flags, files"
  behavior: default

ignore:
  - "**/*_test.go"
  - "examples/**"
  - "benchmarks/**"
